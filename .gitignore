# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Secrets — 절대 커밋 금지
.env
.env.*
!.env.example
.streamlit/secrets.toml
.streamlit/secrets.toml.*
!.streamlit/secrets.toml.example

# Cafe24 OAuth 토큰 (절대 공개 금지)
data/cafe24_tokens.json

# 구버전 주문 파일들 (레거시)
data/*_orders.csv
data/ddokddok_orders.csv
data/rollaru_orders.csv

# orders.csv / reviews.csv — 익명화된 customer_id 로 안전
# git 에 포함하여 Streamlit Cloud 에 배포

# 자동 생성 파일
data/sync_log.txt
data/gh_auth.log
data/gh_auth.log.err
data/*.log

# 광고 데이터는 공개 가능 (개인정보 없음)
# data/ads.csv

# 이미지 캐시는 커밋됨 (이미지 URL 만 포함)
# data/product_images.csv

# 프리컴퓨트 캐시 — Streamlit Cloud 빠른 로드용 (로컬 sync_all.bat 이 매일 갱신 + push)
# data/precomputed/  ← 커밋에 포함 (대시보드 속도 개선)

# 업로드 폴더
data/ezadmin_upload/
data/uploads/

# 쿠팡 광고 원본 CSV/Excel — 파싱 후 precomputed 에 저장됨, 원본은 로컬만
data/coupang_ads_upload/*.xlsx
data/coupang_ads_upload/*.xls
data/coupang_ads_upload/*.csv
!data/coupang_ads_upload/README.txt

# 편집기
.vscode/
.idea/
*.swp
*.swo

# Streamlit 로컬 설정
.streamlit/config.toml.local
